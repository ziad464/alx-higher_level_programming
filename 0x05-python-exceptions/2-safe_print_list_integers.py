#!/user/bin/Python3

def safe_print_list_integers(my_list=[], x=0):
    """print the first x elements of my_list to print.

    Returns:
        The number of elements printed.
    """
    reet = 0
    for i in range(0, x):
        try:
            print("{:d}".format(my_list[i], end="")
                    ret += 1
                    except (ValueError, TypeError):
                    continue
                print("")
                return (ret)
